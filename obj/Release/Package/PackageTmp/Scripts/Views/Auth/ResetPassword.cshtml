@*@Html.Action("Layout", "Main")*@
<div class="reset-password-view view">
    <p>
       @* {{ctrl.resetReason}}*@
        <div>יש להזין סיסמא מבוקשת ואת אותה הסיסמא פעם נוספת</div>
        <div>בסיום התהליך תתעדכן סיסמת המשתמש וניתן יהיה להיכנס למערכת באופן סדיר</div>
    </p>
    <p>
        <span class="label" style=" margin-left: 97px;">סיסמא חדשה</span>
        <input class="passwordText" name="pass1" type="password" ng-model="ctrl.new_password" placeholder="סיסמא חדשה" />
        <div class="field-validation change-pass-view-error" ng-class="{visible:ctrl.invalidNewPassword1}">{{ctrl.invalidNewPassword1Reason}}</div>
    </p>
    <p>
        <span class="label" style="margin-left:56px;">אימות סיסמא חדשה</span>
        <input class="passwordText" name="pass2" type="password" ng-model="ctrl.new_password_2" placeholder="אימות סיסמא חדשה" />
        <div class="field-validation change-pass-view-error" ng-class="{visible:ctrl.invalidNewPassword2}">סיסמאות אינן זהות</div>
    </p>
    <p class="btn-p">
        <button ng-disabled="ctrl.updateBtnDisabled" class="saveBtn btn" ng-click="ctrl.update()" id="submit">עדכון סיסמא</button>
    </p>
</div>
